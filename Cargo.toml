[workspace]
members = ["rust/ocapn_noise", "rust/endo", "rust/endo/xsnap", "rust/endo_iroh"]
# rust/engine is the endor engine port (designs/xs2rust-endor-engine.md),
# its own nested workspace so it builds in-repo from the first commit
# (resolved question 9) without entangling this workspace's members.
exclude = ["rust/engine"]
resolver = "2"
